# Backend API base URL for Frontend build (Vite environment variable)
# When deploying on VPS with Nginx reverse proxy (Same Subdomain varun-traders-billing.gemshine.tech):
# Use relative '/api' so Nginx routes /api/ requests to localhost:5011
VITE_API_URL=/api

# Local Development:
# VITE_API_URL=http://localhost:5011

